<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mks.uuprojectone">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activity.Login_Activity"
            android:label="@string/title_activity_login" />
        <!-- <activity android:name=".Activity.Login_Activity" /> -->
        <activity android:name=".Activity.Activity_Tlogin" />
        <activity android:name=".Activity.Activity_student_signup" />
        <activity android:name=".Activity.Activity_Teacher_Signup" />
        <activity android:name=".Activity.Student_Profile_Activity" />
        <activity android:name=".Activity.Teacher_Profile_Activity" />
        <activity android:name=".Activity.GetAttendance_Activity" />
        <activity android:name=".Activity.Code_Matching_Activity" />
        <activity android:name=".Activity.AttendanceMark_Activity"></activity>
    </application>

</manifest>